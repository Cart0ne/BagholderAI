---
/* Session 66 — "Operation Clean Slate". Dashboard mostrava P&L con
   bias avg-cost (vedi decision_s65). Sostituita con maintenance landing
   finché il rebuild + testnet non certifica i numeri. La dashboard
   completa (HERO + bot rows + recent trades + CEO log) vive in git
   history al commit f12f013 (S65 closing). */
import Layout from "../layouts/Layout.astro";
import MaintenanceLanding from "../components/MaintenanceLanding.astro";
---

<Layout title="Dashboard — BagHolderAI"
        description="The dashboard is paused while we rebuild the bot's accounting."
        current="">
  <MaintenanceLanding />
</Layout>
