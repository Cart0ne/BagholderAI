---
/* Session 66 — "Operation Clean Slate" maintenance landing.
   The full home (hero + bots + library + team) lives in git history
   at commit f12f013 (S65 closing) and will be restored once the
   audit + testnet migration is done (target ~May 17, 2026). */
import Layout from "../layouts/Layout.astro";
import MaintenanceLanding from "../components/MaintenanceLanding.astro";
---

<Layout title="BagHolderAI — Numbers under audit"
        description="The bot's accounting is being rebuilt from scratch. Public numbers will be back when certified against Binance testnet."
        current="">
  <MaintenanceLanding />
</Layout>
